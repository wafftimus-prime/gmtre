import { FormatBirthdayPipe } from './format-birthday.pipe';

describe('FormatBirthdayPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatBirthdayPipe();
    expect(pipe).toBeTruthy();
  });
});
