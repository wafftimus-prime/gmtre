import { JsonDataTypePipe } from './json-data-type.pipe';

describe('JsonDataTypePipe', () => {
  it('create an instance', () => {
    const pipe = new JsonDataTypePipe();
    expect(pipe).toBeTruthy();
  });
});
