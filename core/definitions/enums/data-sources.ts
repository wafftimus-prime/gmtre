export enum eSourceTypeOptions {
  database = 'database',
  file = 'file',
  stream = 'stream',
  api = 'api',

}

export enum eDataTypeOptions {
  boolean = 'boolean',
  string = 'string',
  object = 'object',
  array = 'array'
}

export enum eDatabaseTypes {
  mssql = 'Microsoft SQL Server',
  mysql = 'MySQL',
  postgres = 'PostgreSQL',
  sqlite = 'SQL Lite',
  mariadb = 'MariaDB'
}
